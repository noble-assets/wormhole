package types

const (
	// GuardianSetExpiry defines how long a guardian set should remain active for
	// after being replaced, before then expiring. Currently, 24 hours.
	GuardianSetExpiry = 86400
	AddressLenght     = 20
)
